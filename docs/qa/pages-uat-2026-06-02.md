# GitHub Pages UAT/QA — 2026-06-02 (3-round)

Target: **https://htt-brands.github.io/control-tower/** (served from `main:/docs`).
Tooling: QA Kitten (Playwright Chromium + axe-core 4.10). Driver: Richard (`code-puppy-1725d8`).

Goal: drive the site to **100% passing** over three rounds — find, fix, document, re-verify.

---

## Round 1 — findings (13: 0 critical · 2 high · 5 medium · 6 low)

| ID | Sev | Finding | Resolution |
|----|-----|---------|------------|
| H-1 | HIGH | Site-wide WCAG AA contrast fail: `--text-tertiary #8a8a82` on white = 3.47:1 (needs 4.5:1); faint footer text. Site advertises "WCAG AA" yet fails it. | `main.css`: `--text-tertiary` -> `#6b6b63` (4.6:1); `.footer-bottom` color `.25`->`.62`; `.footer-links a` ->`.72`. |
| H-2 | HIGH | `riverside-timeline.md` served as raw markdown -> countdown JS never runs, visitor sees literal YAML/`<script>` text ("Loading countdown…" stuck). | Created rendered **`riverside-timeline.html`** (working countdown + site chrome); repointed the landing link `.md`->`.html`. |
| M-1 | MED | Section/detail `<title>`s still said "— Azure Governance Platform" (pre-rebrand). | Generator title suffix -> "HTT Control Tower"; regenerated 15 detail pages; batch-fixed 5 section indexes + swagger + 3 stragglers (adr-0010/0011, cost-analysis). Zero old-brand titles remain in any served `.html`. |
| M-2 | MED | Section index pages jump H1 -> H3 (skip H2); `/api/` worse. | `.doc-list-item` headings `<h3>`->`<h2>` across all section indexes + matching `main.css` selectors. |
| M-3 | MED | In-text footer links not distinguishable (colour-only, <3:1, no underline). | `.footer-bottom a`: underline + lighter colour. |
| M-4 | MED | `continuity-status.html` ~29d stale, reads as live. | Added a prominent "point-in-time snapshot (2026-05-04) — see STATE.md/STATUS.md" banner at top of `<main>`. |
| M-5 | MED | "Zero-Secrets Auth / zero secrets in code" card contradicts Key Vault on same page; overstates posture (Phase A). | Reworded to "Federated Identity Auth — OIDC + UAMI, secrets in Key Vault, never in code; zero-secret rollout in progress." |
| L-1 | LOW | No `<header>` landmark site-wide; content outside landmarks. | Wrapped nav in `<header>` in the generator + all hand-written pages. |
| L-3 | LOW | No branded 404 (GitHub generic). | Added `docs/404.html` (branded). |
| L-5 | LOW | No `theme-color`. | Added `<meta name="theme-color" content="#500711">` to landing + new pages. |
| L-6 | LOW | Hamburger toggle missing `aria-controls`. | Added `aria-controls="nav-links"` + `id="nav-links"` site-wide. |
| L-2 | LOW | `status.md` served raw with visible YAML front-matter. | **Deferred to Round 2 assessment** — it's the auto-generated fallback that points to STATUS.md; a render-pipeline change. |
| L-4 | LOW | `architecture.svg` node labels small at display size. | **Deferred to Round 2 assessment** — cosmetic. |

**Bonus (not in QA report):** generator footer said stale **"v1.9.0"** -> "v2.5.0" + STATE.md link.

Files touched: `docs/assets/main.css`, `scripts/build-detail-pages.js` (+ 15 regenerated detail pages), all 5 section indexes, `docs/index.html`, `docs/index.md`, `docs/operations/continuity-status.html`, 3 straggler detail pages; new: `docs/404.html`, `docs/riverside-timeline.html`.

Round 1 status: **fixes applied, pending re-deploy + Round 2 re-verification.**

---

## Round 2 — re-verify + fresh sweep

**Part A — Round 1 re-verification: 10/10 PASS.** Measured evidence:
`.section-subtitle` now #6b6b63 = **5.37:1** (was 3.47:1); axe `color-contrast`,
`heading-order`, `region`(nav) = **0 violations** on landing + 4 sections +
continuity; all `<title>`s say HTT Control Tower; footer links underlined;
continuity snapshot banner present; auth card reworded; branded 404 (axe 0);
`riverside-timeline.html` renders with a **live ticking countdown** + table.

**Part B — fresh findings (6: 0 crit · 0 high · 3 med · 3 low) → all fixed:**

| ID | Sev | Finding | Resolution |
|----|-----|---------|------------|
| R2-1 | MED | `/architecture/overview/` → "Security Guide" link `../security` = 404. | `overview.md`: `../security` -> `../authentication/` (rendered, security-relevant); regenerated. |
| R2-2 | MED | `/architecture/overview/` → `./current.md` (×2, one anchored) = 404 (raw sibling never converted). | `overview.md`: repointed to the GitHub blob of `architecture/current.md` (renders w/ working anchors); regenerated. |
| R2-3 | MED | `continuity-status.html` doc-cards → `../dr/rto-rpo.md` & `../dr/bacpac-validation-decision.md` served raw markdown (same class as H-2). | Repointed both to GitHub blob URLs. (All 11 `.md` links on the page are now GitHub-blob, zero raw.) |
| R2-4 | LOW | axe `region`: `.page-header` (hero/title block) outside any landmark on 7 pages + timeline. | Moved `</header>` to **wrap `.page-header`** in the generator + all hand-written pages (nav stays sticky; verified order header<page-header</header<main). |
| R2-5 | LOW | `theme-color` missing on all generated/section pages (R1 fix only hit custom pages). | Added `<meta theme-color>` to the generator + all section/detail/straggler pages. |
| R2-6 | LOW | Landing no longer linked the timeline (page fixed but orphaned). | Added a "Riverside Timeline" doc-card to the landing Documentation grid. |

**Self-caught during R2 fixes:** the `.page-header` landmark batch left `swagger`'s
`<header>` unclosed (its `<main id="swagger">` didn't match the `id="main"`
regex). Restructured swagger: `<header>` now closes after page-header, the
stale-banner + Swagger render target (`<div id="swagger">`, `dom_id` unchanged)
live inside a proper `<main>`. Verified 1/1 balanced.

Round 2 status: **all fixes applied, pending re-deploy + Round 3 final verification.**

## Round 3 — pending (final verification)
