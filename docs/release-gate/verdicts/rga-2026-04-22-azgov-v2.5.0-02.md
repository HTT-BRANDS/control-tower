# Release Gate Arbiter Verdict — v2.5.0 Re-submission

**Run ID**: `rga-2026-04-22-azgov-v2.5.0-02`
**Prior run**: `rga-2026-04-22-azgov-v2.5.0-01` → `PRECONDITIONS_NOT_MET`
**Artifact**: `azure-governance-platform @ 79d72c4`
**Submitted**: 2026-04-22 by `code-puppy-bf0510`
**Requested transition**: staging-gate (Option A)

## Verdict: `CONDITIONAL_PASS`

Staging-gate transition AUTHORIZED for artifact `79d72c4`.
Production-gate NOT AUTHORIZED — `bd 7mk8` (SLSA L3 + cosign + SBOM) is a hard block.

## Pillars

| Pillar | Result |
|---|---|
| 1. Requirements Closure | `CONDITIONAL_PASS` — RTM retroactive, accepted one-time |
| 2. Code Review | `PASS` |
| 3. Security | `DEGRADED → PASS for staging` (Scanner Group A waived 60d) |
| 4. Infrastructure | `CONDITIONAL_PASS` |
| 5. Stack Coherence | `PASS` |
| 6. Cost | `PASS (with observation)` |
| 7. Maintenance & Operability | `CONDITIONAL_PASS` (single-operator risk flagged) |
| 8. Rollback | `PASS` |

## Conditions (5)

1. **Prod gate requires `7mk8` closed first** — no waiver, no slip to v2.5.1.
2. **If staging promotion fails validation**, treat as `mvxt` recurrence unless `/health` degrades.
3. **v2.5.1 must submit with prospective RTM** — one-time dispensation.
4. **Before advisory→blocking** (2026-06-21):
   - Deterministic Scanner Group A wired (`gitleaks`, `trivy fs`, `osv-scanner`, `semgrep`, `checkov`, `conftest`, `infracost`)
   - env-delta.yaml validator (bd `my5r`)
   - Second human named for rollback co-operation
5. **IaC drift-detection** (bd `x692`, P3).

## Waivers (all disclosed inline, all time-boxed)

| ID | Rule | Reason | Expiry | Renewals |
|---|---|---|---|---|
| `WAV-v2.5.0-7mk8` | P0 Security #1, #2 | fix_started | 2026-07-21 (90d) | 0 |
| `WAV-v2.5.0-mvxt` | RGA-ENV-009 | mitigated_externally | 2026-07-21 (90d) | 1 max |
| `WAV-v2.5.0-scanners` | Scanner Group A absent | evaluating_component | 2026-06-21 (60d) | 0 |
| `WAV-v2.5.0-rtm-retro` | RGA-REQ-005 | acceptable_risk | One-time | N/A |
| `WAV-v2.5.0-envval` | P0 Security #7 | fix_started | 2026-06-21 (60d) | 0 |

## Non-blocking findings (5)

- **N-1** (low): RTM retroactive-only, categorical validation surface → v2.5.1+ prospective.
- **N-2** (low): RTM grep heuristic picks up non-implementation mentions (e.g. `6o4g ↔ a5fce6a`) → filter in next generator.
- **N-3** (info): IaC-vs-runtime-deploy disconnect → bd `x692` (P3) filed.
- **N-4** (low): env-delta.yaml validator absent → bd `my5r` (P2) filed.
- **N-5** (info): `mvxt` still open → rollback plan §5 covers if it recurs on promotion.

## Follow-up tickets filed in response

- `7mk8` P1 — SLSA L3 + cosign + SBOM (pre-existing from submission v2)
- `my5r` P2 — env-delta.yaml validator (NEW, Finding N-4)
- `x692` P3 — Bicep drift detection (NEW, Finding N-3)

---

## Promotion execution record

**Promotion executed**: 2026-04-22T22:16:08Z (auto-triggered by push of `0b0da6b`)
**Outcome**: **SUCCESS** — Deploy to Staging workflow `completed|success` end-to-end.
  - Job 1 QA Gate: success
  - Job 2 Security Scan: success
  - Job 3 Build & Push (with a1sb verify): success
  - Job 4 Deploy → Health gate: success (`/health` 200 on first or subsequent attempt)
  - Job 5 Staging Validation: success (mvxt did NOT recur this run)

**mvxt observation for the run**: On prior commit `2a0aa5c` (also docs-only), Deploy to Staging
failed. On `0b0da6b` five minutes later, Deploy to Staging succeeded. Same immutable image
pathway, opposite outcomes — this is the textbook `mvxt` intermittent-flake fingerprint the
pre-flight guidance described. No code regression.

**Pre-flight corrections applied** (after arbiter's pre-flight guidance run):
- Finding N-6 fixed (env-delta.yaml staging hostname `xncz` → `xnczpwyv`).
- Rollback plan §2, §4 Step 1, §5: image tag format corrected from `:<short7>` to
  `:sha-<full40>` (GHCR retains only the full-SHA form; short-SHA pasting would have
  silently failed in an emergency).
- Rollback plan §5: v2.3.0 secondary fallback replaced with `5c82c71` and a
  rebuild-from-tag emergency path, because GHCR retention has pruned pre-2026-04-18
  images.

**Staging-gate status**: TRANSITIONED. Artifact `79d72c4` is now the staging-deployed version.
**Waiver clocks**: Unchanged. Advisory→blocking transition still on 2026-06-21 for `my5r` +
scanner wiring; 2026-07-21 for `7mk8` + `mvxt`.
